# Apex Cab server configuration (all optional; sensible defaults shown)
PORT=3000
PUBLIC_BASE_URL=http://localhost:3000
ALGOD_URL=https://testnet-api.algonode.cloud
FACILITATOR_URL=https://facilitator.goplausible.xyz
DISPENSER_URL=https://dispenser.testnet.algokit.io
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Optional: override the auto-generated Testnet wallets (base64 of 64 bytes).
# Leave unset and the server generates + persists them under data/.
# MERCHANT_PRIVATE_KEY=
# PAYER_PRIVATE_KEY=
